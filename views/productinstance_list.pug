extends layout

block content
    .content
        if (inventory.length > 0)
            each item in inventory 
                p= item.name
        else 
            h1 Nothing in stock.

        a.link-button(href="/inventory/create") Add product to inventory
extends layout

block content
    .content
        form(method='POST').add-form
            div.form-group
                label(for='name') Product Name:
                input#name.form-control(
                type='text' 
                placeholder='Product name' 
                name='name' 
                required 
                value=(undefined===product 
                    ? '' 
                    : product.name) )  
            div.form-group
                label(for='model') Model:
                input#name.form-control(
                type='text' 
                placeholder='Model name' 
                name='model' 
                required 
                value=(undefined===product 
                    ? '' 
                    : product.model) ) 
            div.form-group
                label(for='model') Description:
                textarea#name.form-control(
                type='text' 
                name='description' 
                rows='6'
                cols='20'
                required 
                )  #{undefined===product ? '' : product.description}
            div.form-group
                label(for='processingType') Type:
                datalist(id = "processingTypes")
                    option(value="DSP") 
                    option(value="Analog") 
                    option(value="Hybrid")
                input#name.form-control(
                list='processingTypes' 
                placeholder='DSP/Analog/Hybrid' 
                name='processingType' 
                required 
                value=(undefined===product 
                    ? '' 
                    : product.processingType) )  
            div.form-group
                label(for='channels') Channels:
                input#name.form-control(
                type='numeric' 
                placeholder='12' 
                name='channels' 
                required 
                value=(undefined===product 
                    ? '' 
                    : product.channels) )  
            div.form-group
                label(for='price') Price:
                input#name.form-control(
                type='numeric' 
                placeholder='599.99' 
                name='price' 
                required 
                value=(undefined===product 
                    ? '' 
                    : product.price) )  
            div.form-group
                label(for='releaseDate') Release Date:
                input#releaseDate.form-control(
                type='date' 
                name='releaseDate' 
                required
                value=(undefined===product 
                ? today 
                : product.releaseDate_ISO) )
            button.btn.btn-primary(type='submit') Submit
    if errors
        ul
            for error in errors
                li!= error.msg
